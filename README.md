# BusMate Lanka

React + Vite + Tailwind CSS v4 prototype with route search, seat selection and simulated live GPS tracking.

## Run

```bash
npm install
npm run dev
```

Open the URL printed by Vite, normally http://localhost:5173.

## Production build

```bash
npm run build
npm run preview
```

The production files are generated in `dist/`.
